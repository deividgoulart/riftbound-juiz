# Copie para ".env.local". Endereço da API (api/main.py). No seu computador: uvicorn na porta 8000.
# Na Vercel, a variável é o endereço da API no Render, ex.: https://juiz-riftbound-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:8000
