# Tudo do app + o que só serve pra desenvolver: notebooks, testes e comparações.
# Instalação: pip install -r requirements-dev.txt
-r requirements.txt

pandas>=3.0          # tabelas das avaliações e dos notebooks
matplotlib>=3.11     # gráficos dos notebooks
jupyter>=1.1         # roda os notebooks
pytest>=9.0          # roda os testes em tests/ (python -m pytest)
rank-bm25>=0.2       # busca por palavra-chave, a linha de base da etapa 4
