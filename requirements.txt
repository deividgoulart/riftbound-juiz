# Etapa 1: download e exploração do FAQ (versões testadas em 23/09/2026)
python-dotenv>=1.2   # lê as chaves de API do arquivo .env
pyyaml>=6.0          # lê o frontmatter (cabeçalho YAML) dos arquivos MDX
pandas>=3.0          # tabelas e contagens na exploração
matplotlib>=3.11     # gráficos do notebook
jupyter>=1.1         # roda o notebook de exploração

# Etapa 2: testes automatizados
pytest>=9.0          # roda os testes em tests/ (python -m pytest)

# Etapa 3: leitura do CRD
beautifulsoup4>=4.13 # lê o HTML do CRD e navega pela árvore de regras

# Etapa 4: embeddings e busca
numpy>=2.0                  # guarda os vetores e calcula a similaridade
torch>=2.6                  # roda os modelos locais (no Windows, o pacote do PyPI já é a versão CPU)
sentence-transformers>=5.0  # carrega os modelos de embeddings do HuggingFace
google-genai>=1.30          # SDK oficial do Gemini (modelo gemini-2 da comparação)
rank-bm25>=0.2              # busca por palavra-chave, usada como linha de base

# Etapa 6: interface
streamlit>=1.64             # o app de chat (streamlit run app.py)
